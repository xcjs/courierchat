services:
  courierchat:
    build:
      context: .
      target: deps
    working_dir: /app
    command: npm run dev -- --host 0.0.0.0
    ports:
      - "3000:3000"
      - "3478:3478/udp"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.nuxt
      - /app/.output